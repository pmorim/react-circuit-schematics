import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

<Meta title='Advanced/Intro' />

# Core elements

All electrical components are created by these core components. Think of them as small building blocks that together create a component. You can easily create your own custom components if you feel that the default ones don't fit your application well enough.
